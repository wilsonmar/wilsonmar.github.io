---
layout: post
date: "2025-08-02"
lastchange: "v027 + intro hands-on sequence :2011-12-28-dns.md"
url: "https://wilsonmar.github.io/dns"
file: "dns"
title: "DNS (Domain Name System)"
excerpt: "Translate domain names to IP addresses"
tags: [Database, Mac]
image:
# feature: pic white robots woman 1900x500.jpg
  feature: https://cloud.githubusercontent.com/assets/300046/14622167/45abd918-0585-11e6-8537-a58e0b55e3ec.jpg
  credit: Cyberconstruct.be
  creditlink: http://cyberconstruct.be/2015/02/digital-job-crafting/
comments: true
created: "2011-12-28"
---
<i>{{ page.excerpt }}</i>
{% include l18n.html %}
{% include _toc.html %}

The DNS (Domain Name System) is like a Contact List of <strong>IP address</strong> for physical locations on the internet.

## Register a website

<a target="_blank" href="https://www.youtube.com/watch?v=U-i_UDDYLxY">VIDEO</a> "What are DNS Zones and Records?"
This information is defined in a <strong>DNS Zone file</strong> created when those who register a website name with a Registrar such as GoDaddy, Google Domains, etc.

These are propagated to DNS servers around the world.

An SOA (Start of Authority) record defines which registrar is in charge of each domain name.

## Ping host name for IP address

1. Open a Terminal app.
1. Try to use the ping utility:
   ```
   ping gogle.com
   ```
   If the server has a ping service, the response would have the IP address:
   ```
   PING gogle.com (142.251.188.105): 56 data bytes
   ```

## DNS Resolution

From <a target="_blank" href="https://www.youtube.com/watch?v=27r4Bzuj5NQ">VIDEO by ByteByteGo</a>:<br />
<a target="_blank" href="https://res.cloudinary.com/dcajqrroq/image/upload/v1754115271/dns-bytebytego-1576x601_maiilw.png"><img alt="dns-bytebytego-1576x601.png" src="https://res.cloudinary.com/dcajqrroq/image/upload/v1754115271/dns-bytebytego-1576x601_maiilw.png" /></a>

1. On the address bar of an internet browsers such as Chrome and Firefox, if you type an IP address it will send a request to that address. But if you type in a <strong>host name</strong> such as google.com, the DNS (Domain Name System) program resolves host names (such as google.com) to their <strong>IP address</strong> where websites reside.

2. The DNS (Domain Name System) utility program on your machine first checks its <strong>cache</strong> saved from a recent lookup.

   BTW If the host name has not been resolved recently, a DNS <strong>recursive resolver</strong> is called recursively because it follows several steps:

3. DNS requests the IP address of each host name's <strong>TLD (Top Level Domain)</strong> such as .com, .edu, .org, .io, .net, .gov, and country domains (such as .au for australia, etc.). They are managed by the IANA (Internet Assigned Numbers Authority).

   DNS servers obtain IP address from URL names by forwarding requests it cannot resolve from its own tables.

4. DNS receives the <strong>root server</strong> address.
5. Your DNS program goes to the specific root server to get
6. the "authoritative" name server for the host name, which is 
7. queried for 
8. the desired site's IP address on the authoritative server.
9. Your DNS program would attempt to access the desired website's IP address.

Referenes:
   * <a target="_blank" href="https://www.youtube.com/watch?v=UVR9lhUGAyU">VIDEO from Fireship</a>:
   * <a target="_blank" href="https://www.youtube.com/watch?v=g6R9gRWIIK8">VIDEO by The Cloud Girl</a>: "What is * <a target="_blank" href="https://www.youtube.com/watch?v=nyH0nYhMW9M">VIDEO</a>: "DNS? | How a DNS Server (Domain Name System) works | DNS Explained"
   * <a target="_blank" href="https://www.youtube.com/watch?v=uOfonONtIuk">VIDEO from Computerphile</a>:
   * <a target="_blank" href="https://www.youtube.com/watch?v=FsGUi5pXpLk">VIDEO</a>
   * <a target="_blank" href="https://www.youtube.com/watch?v=Azn7KF9dB8s&pp=0gcJCccJAYcqIYzv">VIDEO</a> "
   DNS Explained | Domain Name System | Cisco CCNA 200-301" by CertBros

DNS also resolves each host name in web pages.

IP address to some websites we don't want to visit, which return content that are malicious or a waste of time.

## What DNS server do you use?

1. Open a Terminal app.
1. Display the configuration file:
   ```
   scutil --dns
   ```
   Example response:
   ```
   resolver #1
   nameserver[0] : 9.9.9.9
   nameserver[1] : 149.112.112.112
   if_index : 14 (en0)
   flags    : Scoped, Request A records
   reach    : 0x00000002 (Reachable)
   ```
1. Alternately:
   ```
   networksetup -getdnsservers Wi-Fi
   networksetup -getdnsservers Ethernet
   ```
   Response:
   ```
   9.9.9.9
   149.112.112.112
   Ethernet is not a recognized network service.
   ** Error: The parameters were not valid.
   ```
PROTIP: "9.9.9.9" and "149.112.112.112" are at <a target="_blank" href="https://isc.sans.edu/suspicious_domains.html">isc.sans.edu/suspicious_domains.html</a> operated by <a target="_blank" href="https://www.quad9.net/support/set-up-guides/mac-os/">Quad9</a> (a non-profit based in Switzerland). Quad9 aims to not resolve to known malicious domains found by UK Scotland Yard and other affiliated law enforcement agencies. Quad9 maintains and enforces a <strong>blocklist</strong> (blacklist) of known malicious websites.

Quad9 also does not <strong>track and sell</strong> your (their users') DNS resolution history.
Tracking is why DNS addresses recommended by ISPs (Internet Service Providers) such as AT&T, Verizon, T-Mobile, etc. are <strong>slower</strong>. <a target="_blank" href="https://www.youtube.com/watch?v=WyVbEsmAV84">VIDEO</a>: Lawrence Systems uses Steve Gibson's DNS Benchmark to show that Google and Quad9 DNS has similar speed.

<a target="_blank" href="https://youtu.be/imlFubYv8YY&t=4m55s">A script to test several DNS</a> showed:
   * 9.9.9.9 # from Quad9 filtered 96% of known malicious IP addresses
   * 1.1.1.2 # Cloudflare Malware filtered 57%
   * 103.247.36.36 # DNSFilter.com (paid service) filtered 16%
   * 208.67.222.222 # OpenDNS (paid service bought by Cisco Umbrella) filtered 9%

   * 8.8.8.8 # from Google does not filter
   * 1.1.1.1 # from Cloudflare does not filter
   <br /><br />

Still, all DNS service tables can be <strong>poisoned</strong> to route users to malicious IP addresses. [<a target="_blank" href="https://www.youtube.com/watch?v=7MT1F0O3_Yw">VIDEO</a>]


## Configure DNS on macOS

To configure DNS on MacOS: <a target="_blank" href="https://www.youtube.com/embed/NM4AdfEnAmY">VIDEO</a>:

1. Press control+command+, (comma) for System Preferences
2. Type Network
3. Advanced
4. DNS
5. Click the "+"
6. Type 9.9.9.9
7. Add another: 149.112.112.112
8. OK
9. Apply
10. command+Q to dismiss the dialog by clicking the red icon at the upper-left corner.
<br /><br />


## local

A better alternative is to have a local <strong>Recursive DNS server</strong> which enforces an <strong>allowlist</strong> (whitelist) of assumed-good host names and IP addresses (such as medicare.gov). <a target="_blank" href="https://www.youtube.com/watch?v=FnFtWsZ8IP0">VIDEO</a>


## Dig name from Google's DNS

1. To identify the domain name associated with the IP address for Google's DNS server:

   <pre><strong>dig -x 8.8.8.8 </strong></pre>

   Under "ANSWER SECTION:" is "google":

   <pre>; &LT;&LT;>> DiG 9.10.6 &LT;&LT;>> -x 8.8.8.8
;; global options: +cmd
;; Got answer:
;; -&LT;&LT;HEADER&LT;&LT;- opcode: QUERY, status: NOERROR, id: 6599
;; flags: qr rd ra; QUERY: 1, ANSWER: 1, AUTHORITY: 0, ADDITIONAL: 1
&nbsp;
;; OPT PSEUDOSECTION:
; EDNS: version: 0, flags:; udp: 4096
;; QUESTION SECTION:
;8.8.8.8.in-addr.arpa.		IN	PTR
&nbsp;
;; ANSWER SECTION:
8.8.8.8.in-addr.arpa.	300	IN	PTR	<strong>dns.google</strong>.
&nbsp;
;; Query time: 64 msec
;; SERVER: 10.11.73.1#53(10.11.73.1)
;; WHEN: Thu Apr 09 13:16:59 MDT 2020
;; MSG SIZE  rcvd: 93
   </pre>

   For Microsoft.com, the response is "prd1.azuredns-cloud.net. msnhst.microsoft.com":

   For apple.com, apple.com, ipad.host, appleid.org, pv-apple-com.apple.com, apple.by

1. To identify the domain name associated with the ip address for the Quad9 DNS service:

   <pre><strong>dig -x 9.9.9.9 </strong></pre>

   <pre>; &LT;&LT;>> DiG 9.10.6 &LT;&LT;>> -x 9.9.9.9
;; global options: +cmd
;; Got answer:
;; ->>HEADER&LT;&LT;- opcode: QUERY, status: NOERROR, id: 2573
;; flags: qr rd ra; QUERY: 1, ANSWER: 1, AUTHORITY: 0, ADDITIONAL: 1
&nbsp;
;; OPT PSEUDOSECTION:
; EDNS: version: 0, flags:; udp: 1232
; OPT=15: 00 17 5b 32 30 30 31 3a 35 30 30 3a 31 34 3a 36 30 30 34 3a 61 64 3a 3a 31 5d 3a 35 33 20 74 69 6d 65 64 20 6f 75 74 20 66 6f 72 20 39 2e 39 2e 39 2e 69 6e 2d 61 64 64 72 2e 61 72 70 61 20 44 4e 53 4b 45 59 ("..[2001:500:14:6004:ad::1]:53 timed out for 9.9.9.in-addr.arpa DNSKEY")
;; QUESTION SECTION:
;9.9.9.9.in-addr.arpa.		IN	PTR
&nbsp;
;; ANSWER SECTION:
9.9.9.9.in-addr.arpa.	169406	IN	PTR	dns9.quad9.net.
&nbsp;
;; Query time: 46 msec
;; SERVER: 192.168.1.1#53(192.168.1.1)
;; WHEN: Thu Mar 17 03:25:37 MDT 2022
;; MSG SIZE  rcvd: 150
   </pre>


<hr />

## ICMP Ping

1. To identify the IP address of a domain name such as google.com:

   <pre><strong>ping google.com</strong></pre>

   NOTE: Because this can be a vector for denial of service attacks and to limit network reconissance by malicious actors, many organizations disable ping responses through ICMP protocol:

   A sample response returned:

   <pre>PING google.com (216.58.193.206): 56 data bytes
64 bytes from 142.250.217.110: icmp_seq=15 ttl=119 time=45.650 ms
^C
--- google.com ping statistics ---
16 packets transmitted, 16 packets received, 0.0% packet loss
round-trip min/avg/max/stddev = 45.650/68.117/85.318/12.225 ms
   </pre>


## Name Servers

Clients -- called <strong>resolvers</strong> -- make requests of DNS <strong>name servers</strong>.

Two DNS servers are usually specified (in client machine TCP/IP properties) for load balancing and fault tolerance.
 
DNS servers refer to 3 types of records to answer 3 types of queries:

* <strong>A</strong> (host Address) records are used to answer <strong>forward lookup</strong> 
	of an FQDN (host name) to a specific IP address.
	
	On Windows, the host name to IP address mappings for a zone are stored in the <strong>Domain.dns</strong> file
	in the <a href="1envvars.htm">&#37;systemroot&#37;</a>\System32\Dns folder.
 
<a name="DNSPTR"></a>
* <strong>PTR</strong> (<span style="text-decoration:underline">P</span>oin<span style="text-decoration:underline">t</span>e<span style="text-decoration:underline">r</span> resource) records are used to 
	answer a <strong>reverse lookup</strong> of an IP address to a host name
	(another DNS domain name location).
	IP address to host name mappings are in the <tt><strong>z.y.w.x.in-addr.arpa</strong></tt> file.
	Create file <strong>1.0.0.127.in-addr.arpa</strong> zone file for reverse lookup.
 
* <strong>SRV</strong> (<span style="text-decoration:underline">S</span>e<span style="text-decoration:underline">rv</span>er location) records are used to <strong>locate domain controllers</strong>.
	SRV specifies the server to which a DNS name server <strong>forwards</strong> 
	when it cannot resolve a query.

	Windows servers <strong>require DNS</strong> to locate domain controllers.
	On Windows, DNS is installed as a Windows component on a 
	domain controller with a static (not dynamic) IP address.
 
Other types of resource records:

* <strong>NS</strong> records notate which DNS servers are designated 
		as authoritative for the zone.
 
* <strong>SOA</strong> (Start Of Authority) records indicate the <strong>name of origin</strong> and other basic properties for each zone,
		including the name of the primary server for the source for information about the zone,
 
* <strong>CNAME</strong> (Cononical name) records define <strong>aliases</strong>.
 
* <strong>MX</strong> (<span style="text-decoration:underline">M</span>ail e<span style="text-decoration:underline">x</span>changer) records define the <strong>preference number</strong>.

 
## Zones

There is a separate set of DNSNode records for <strong>each DNSZone</strong> handled by a DNS server.

DNS only resolves queries for zones to which it has <strong>authority</strong>.

Zones are part of a hierarchial structure of 
<a href="#TLDs">top-level domains<img src="http://merc.tv/img/itemsep.gif" width="11" height="11" border="0"></a>
over <strong>Second-level domains</strong> (represented by the organization's name, such as Microsoft or Compaq),
etc. 
 
<table align="right" cellpadding="4" cellspacing="0" border="1"><thead>
</thead><tbody>
<tr><th> Standard </th><th> Active Directory Integrated </th></tr>
<tr valign="top"><td> DNSZone </td><td> Container Objects </td></tr>
<tr valign="top"><td> DNSNode </td><td> Leaf Objects </td></tr>
</tbody></table>

<strong>DNS zone</strong> data can be stored in <strong>text files</strong> on Primary or Secondary domain controllers.
This is under the <strong>single master model</strong>.

Alternately, you can change	a Windows 2000 DNS service to use the <strong>Active Directory Integrated</strong> database.
Replication of AD data to other servers makes this a <strong>multi-master</strong> model.
 
This is done using the <strong>DNS Console Manager</strong> GUI -- 
in a zone's Properties dialog box General tab, click the change button.

Alternately, use a script to issue sub-commands from command interface

<tt><img src="http://merc.tv/img/cmd.gif"> nslookup.exe
<br />Default Server: &nbsp; rns2.earthlink.net
<br />Address: 207.217.77.82
</tt>
 
AD Integration is reversed in the <strong>Advanced tab</strong> by setting the &#8220;Load Data On Startup&#8221; field to &#8220;From Registry&#8221;.


## Configuration

One no longer directly edit <tt>/etc/resolv.conf</tt>

On Ubuntu, within <tt>/etc/network/interfaces</tt>

   <pre>dns-namservers 192.168.1.1. 8.8.8.8
dns-search example.com
   </pre>

Reload network using <tt>/etc/init.d/networking reload</tt>

1. On Red Hat, write to interface:

   <tt>/etc/sysconfig/network-scripts/ifcfg-eth0</tt>

   DNS1=8.8.8.8<br />
   DNS2=192.168.1.1

1 To activate:

   <pre><strong>service network restart</strong></pre>

   <pre><strong>system ctl restart NetworkManager</strong></pre>

   Response "NXDOMAIN" means that the domain name was not found.

<hr />

## Windows DNS

<img src="http://merc.tv/img/tool.gif" width="16" height="16" alt="tool" />
Use the <strong>Windows Network Monitor</strong> to monitor and capture 
packets sent to and from DNS servers.
 
<img src="http://merc.tv/img/tool.gif" width="16" height="16" alt="tool" />
DNS server administration can be done by a script using this utility from the Windows Server product CD:

<ul><tt><img src="http://merc.tv/img/cmd.gif"> DNSCMD.EXE</tt></ul>
 
Servers send an A (resource) record to DNS when it first boots up.
To force a member server to register itself to DNS:

   <ul><tt><strong><img src="http://merc.tv/img/cmd.gif"> ipconfig /registerdns </strong></tt></ul>
 
## Event Log

<img src="http://merc.tv/img/tool.gif" width="16" height="16" alt="tool" />
In the <strong>Event Log</strong>, the DNS log file shows DNS request activities.
But it doesn't show specific zone property changes
or information about existing zone transfers.
 
Caching-only DNS servers work from data cached while resolving queries 
using server in <strong>Root Hints</strong> stored in the <strong>Cache.dns</strong> file rather than from DNS zone transfers.

<a target="_blank" href="https://www.youtube.com/watch?v=rPpTi3oZ6_4">VIDEO</a>: "The Ultimate Pi-Hole Installation Guide for 2025!" by Mackey Tech IT Solutions